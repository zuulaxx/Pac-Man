import { GameBoard } from '@/components/GameBoard';

const Index = () => {
  return (
    <main className="min-h-screen bg-background flex items-center justify-center p-2 sm:p-4 md:py-8 overflow-x-hidden">
      <GameBoard />
    </main>
  );
};

export default Index;
